node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/
motion-audit/
.capture/
_*.mjs
tests/fixtures/sw-freshness.html

# Android build outputs. The project sources under android/ are tracked; everything Gradle
# generates is not, and neither is the SDK it needs.
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.apk
