{
  "name": "xancode-os",
  "private": true,
  "version": "1.0.0",
  "description": "XanCode OS - premium iOS-style home screen for scanning, generating and organizing barcodes and QR codes. Self-contained single-file PWA: index.html plus vendored assets under ./vendor, no CDN and no build step required to run it. This package.json exists to run the Playwright regression suite and to rebuild vendor/tailwind.css.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "node scripts/dev-server.mjs",
    "build:css": "tailwindcss -c tailwind.config.js -i vendor/tailwind.src.css -o vendor/tailwind.css --minify",
    "audit:motion": "node scripts/motion-audit.mjs",
    "test:camera": "playwright test tests/camera.spec.js tests/camera-1d.spec.js",
    "test:offline": "playwright test tests/pwa.spec.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jerjerrydrive-code/fluffy-umbrella.git"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "tailwindcss": "^3.4.19"
  }
}
